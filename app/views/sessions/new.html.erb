<div class="container">
  <div class="hero" style="padding: 3rem 0;">
    <div class="container">
      <h1 style="font-size: 2.5rem;">🔑 Welcome Back</h1>
      <p style="font-size: 1.1rem;">Sign in to your ShelfShare account to continue sharing books</p>
    </div>
  </div>

  <div class="main-content" style="max-width: 400px; margin: -2rem auto 0 auto;">
    <%= form_with url: sessions_path, local: true, html: { class: "login-form" } do |form| %>
      <% if flash[:alert] %>
        <div class="alert alert-danger mb-4">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <div class="form-group">
        <%= form.label :email, "📧 Email Address", class: "form-label" %>
        <%= form.email_field :email, 
            required: true,
            class: "form-control",
            placeholder: "Enter your email address",
            autofocus: true %>
      </div>

      <div class="form-group">
        <%= form.label :password, "🔒 Password", class: "form-label" %>
        <%= form.password_field :password, 
            required: true,
            class: "form-control",
            placeholder: "Enter your password" %>
      </div>

      <div class="form-group mt-4">
        <%= form.submit "🚀 Sign In", class: "btn btn-primary btn-lg" %>
      </div>

      <div class="text-center mt-4">
        <div style="background: var(--light-gray); padding: 1rem; border-radius: var(--border-radius);">
          <p class="mb-2 text-secondary">New to ShelfShare?</p>
          <%= link_to signup_path, class: "btn btn-success" do %>
            📚 Create Account
          <% end %>
        </div>
      </div>

      <div class="text-center mt-4">
        <small class="text-secondary">
          Join thousands of book lovers sharing their favorite reads with their communities.
        </small>
      </div>
    <% end %>
  </div>

  <div class="text-center mt-5">
    <%= link_to root_path, class: "btn btn-secondary" do %>
      ← Back to Home
    <% end %>
  </div>
</div>